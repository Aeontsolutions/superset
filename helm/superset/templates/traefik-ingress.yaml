apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: superset-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: analyticsaws-dev.aeontsolutions.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: custom-superset-dev
            port:
              number: 8088