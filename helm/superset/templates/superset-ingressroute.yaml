apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: superset-ingressroute
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - match: Host(`analyticstwo-dev.aeontsolutions.com`)
    kind: Rule
    services:
    - name: custom-superset-dev
      port: 8088
  tls:
    certResolver: default
    domains:
    - main: analyticstwo-dev.aeontsolutions.com
